# 🚀 List-Pro

مشروع **List‑Pro** هو تطبيق ويب مبني بأسلوب **Full‑Stack** باستخدام:

* ⚛️ **React** للواجهة الأمامية (Frontend)
* 🧠 **Laravel** كـ API للواجهة الخلفية (Backend)

الهدف من المشروع هو تقديم بنية واضحة وقابلة للتطوير لتطبيقات تعتمد على React مع API احترافي مبني بـ Laravel.

---

## 📂 هيكل المشروع

```
list-pro/
├── frontend/        # مشروع React (واجهة المستخدم)
│   ├── src/
│   ├── public/
│   └── package.json
│
├── backend/         # مشروع Laravel (API)
│   ├── app/
│   ├── routes/
│   ├── database/
│   └── artisan
│
├── .gitignore
└── README.md
```

---

## 🛠️ المتطلبات

قبل تشغيل المشروع تأكد من توفر التالي على جهازك:

* Node.js (v18 أو أحدث)
* npm أو yarn
* PHP (8.1 أو أحدث)
* Composer
* MySQL
* Git

---

## ⚙️ تشغيل المشروع محليًا

### 1️⃣ تشغيل Laravel API (Backend)

```bash
cd backend
composer install
cp .env.example .env
php artisan key:generate
```

* عدل بيانات قاعدة البيانات في ملف `.env`
* أنشئ قاعدة بيانات فارغة في MySQL

ثم شغّل السيرفر:

```bash
php artisan serve
```

سيعمل الـ API عادة على:

```
http://127.0.0.1:8000
```

---

### 2️⃣ تشغيل React (Frontend)

```bash
cd frontend
npm install
npm start
```

سيعمل الموقع على:

```
http://localhost:3000
```

> تأكد أن رابط الـ API مضبوط داخل React (مثلاً في ملف env أو config).

---

## 🔐 الأمان والملفات الحساسة

تم تجاهل الملفات الحساسة تلقائيًا عبر `.gitignore` مثل:

* `backend/.env`
* `backend/vendor/`
* `frontend/node_modules/`

⚠️ لا تقم أبدًا برفع هذه الملفات إلى GitHub.

---

## 🌍 النشر (Deployment)

* **Frontend (React):**

  * GitHub Pages
  * Vercel
  * Netlify

* **Backend (Laravel API):**

  * InfinityFree
  * Render
  * Railway

يمكن ربط الواجهة الأمامية مع الـ API عبر URL مباشر.

---

## 🤝 المساهمة

أي مساهمة مرحب بها 🙌:

1. Fork للمستودع
2. إنشاء فرع جديد
3. تنفيذ التعديلات
4. Pull Request

---

## 👤 المطوّر

* GitHub: [https://github.com/saadabdulmilksaad-svg](https://github.com/saadabdulmilksaad-svg)

---

⭐ إذا أعجبك المشروع لا تنسَ وضع Star على المستودع!
